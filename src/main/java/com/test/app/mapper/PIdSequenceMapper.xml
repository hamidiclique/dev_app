<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.test.app.dao.PIdSequenceMapper">

	<select id="getPIdNextval" resultType="int">
		SELECT PID_SEQ.NEXTVAL FROM DUAL
	</select>
	
	<select id="getPIdCurrval" resultType="int">
		SELECT PID_SEQ.CURRVAL FROM DUAL
	</select>
	
</mapper>